name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: сmake test
      run: mkdir build && cd build && сmake .. && make && cd ..
    - name: cd sys
      run: cd kinda_system
    - name: сmake sys
      run: mkdir build && cd build && сmake .. && make && cd ..
