name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: сmake test
      run: сmake
    - name: make test
      run: make
    - name: cd
      run: cd kinda_system
    - name: сmake sys
      run: сmake
    - name: make sys
      run: make
